+ mp3 support
+ module support
+ finish low level GLU support (GLU objects)
+ higher level opengl support
+ higher level glu support
+ make a generic class (like surface), that supports both sdl and opengl
* rewrite font code, etc to use the new generic surface
* check GL constants against *nix versions
* create patch against libsndfile for easy updating
* all the TODOs everywhere in the code!
* Add "real" support for overlapping windows in the windowing code